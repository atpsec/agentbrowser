name: Validate Studio gallery v2 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Studio gallery v2 is archived; current validation is validate-studio-gallery-v4.yml.'
